<!DOCTYPE html>
<html>
<head>
  <title>Prediction Result - AgroPal</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/style.css" rel="stylesheet">
  <style>
    body {
      background-color: #f4fff3;
    }
    .result-card {
      background: white;
      padding: 30px;
      border-radius: 20px;
      box-shadow: 0 0 20px rgba(0,0,0,0.1);
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-dark bg-success mb-4 shadow">
    <div class="container">
      <a class="navbar-brand fw-bold fs-3" href="/">AgroPal</a>
    </div>
  </nav>

  <div class="container d-flex justify-content-center">
    <div class="result-card text-center w-100" style="max-width: 600px;">
      <% if (prediction && !prediction.toLowerCase().includes("error")) { %>
        <h2 class="mb-4 text-success">💧 Predicted Irrigation Requirement</h2>
        <div class="alert alert-info fs-4">
          Required Irrigation: <strong><%= prediction %> mm</strong>
        </div>
      <% } else { %>
        <h2 class="mb-4 text-danger">⚠️ Prediction Failed</h2>
        <div class="alert alert-danger fs-5">
          <strong><%= prediction %></strong><br>
          Please check your inputs and try again.
        </div>
      <% } %>

      <a href="/" class="btn btn-outline-success mt-3">🔁 Predict Again</a>
    </div>
  </div>
</body>
</html>
